<?xml version='1.0' encoding='UTF-8'?>
<xwikidoc version="1.1">
  <web>Blog</web>
  <name>Migration</name>
  <language/>
  <defaultLanguage/>
  <translation>0</translation>
  <creator>xwiki:XWiki.Admin</creator>
  <creationDate>1429197412000</creationDate>
  <parent>Blog.Management</parent>
  <author>xwiki:XWiki.Admin</author>
  <contentAuthor>xwiki:XWiki.Admin</contentAuthor>
  <date>1229214636000</date>
  <contentUpdateDate>1229214636000</contentUpdateDate>
  <version>1.1</version>
  <title>Blog migration</title>
  <comment>Imported from XAR</comment>
  <minorEdit>false</minorEdit>
  <syntaxId>xwiki/2.0</syntaxId>
  <hidden>true</hidden>
  <content>{{velocity filter="none"}}
#if($request.migrate &amp;&amp; $!{services.csrf.isTokenValid("$!{request.getParameter('form_token')}")})
  #set($newContent = '#includeForm("Blog.BlogPostSheet")')
  #set($query = ", BaseObject obj where obj.name = doc.fullName and obj.className = 'XWiki.ArticleClass'")
  #foreach($article in $services.query.hql($query).execute())
    #if($xwiki.hasAccessLevel('edit', $xcontext.user, $article))
      #set($articleDoc = $xwiki.getDocument($article))
    #set($articleObj = $articleDoc.getObject('XWiki.ArticleClass'))
      #set($entryObj = $articleDoc.newObject('Blog.BlogPostClass'))
      #foreach($property in ['title', 'content', 'extract', 'category'])
        $!entryObj.set($property, $articleObj.getProperty($property).value)
      #end
      $!entryObj.set('published', 1)
      $!entryObj.set('publishDate', $articleDoc.creationDate)
      $!entryObj.set('hidden', 0)
      #set($discard = $articleDoc.removeObjects('XWiki.ArticleClass'))
      $articleDoc.setContent($newContent)
      $!articleDoc.save($services.localization.render('xe.blog.migration.migrated'), true)
      * $services.localization.render('xe.blog.migration.updated') [[$entryObj.title>>${article}]] ($services.localization.render('xe.blog.migration.inspace') [[$articleDoc.space>>${articleDoc.space}.WebHome]])
    #else
      * $services.localization.render('xe.blog.migration.skipping') [[$article]]
    #end
  #end
  $services.localization.render('xe.blog.migration.done') [[$services.localization.render('xe.blog.migration.backtoblog')>>Blog.WebHome]]
#else
$services.localization.render('xe.blog.migration.pleaseconfirm')

{{html clean="false" wiki="true"}}
  &lt;form action="$doc.getURL()" method="post">
    &lt;div>
    &lt;input type="hidden" name="form_token" value="$!{services.csrf.getToken()}" />
    &lt;input type="submit" name="migrate" value="$services.localization.render('xe.blog.migration.confirm')"/>
    &lt;/div>
  &lt;/form>
{{/html}}

[[$services.localization.render('xe.blog.migration.backtoblog')>>Blog.WebHome]]
#end
{{/velocity}}</content>
</xwikidoc>